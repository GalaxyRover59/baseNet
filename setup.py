from setuptools import setup, find_packages

setup(
    name="baseNet",
    version="0.2",
    description="A graph neural network project that only includes basic functions in PyTorch.",
    author="Huang",
    packages=find_packages(),
    # install_requires=[
    # ],
)
